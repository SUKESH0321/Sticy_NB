// NoteWindowManager - placeholder module for potential future multi-window support
// Currently not used; the app uses a single-window approach with localStorage persistence.
export const NoteWindowManager = {
  createNoteWindow: (_noteId) => null,
  deleteNoteWindow: (_id) => {},
  focusNoteWindow: (_id) => {},
  restoreWindows: () => {},
};